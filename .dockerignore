# VCS
.git
.gitignore

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# Local env / secrets — НЕ должны попадать в образ
.env
.env.*
!.env.example

# Logs and exports (монтируются как volume, в образе не нужны)
logs/
*.log
exports/
*.txt
!requirements.txt

# Dev / IDE
.vscode/
.idea/
.pytest_cache/
.mypy_cache/

# Docker
docker-compose*.yml
docker-compose.override.yml.example
Dockerfile

# Docs (не нужны в рантайме)
*.md
